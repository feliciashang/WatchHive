import type { HtmlTagDescriptor } from 'vite';
import type { Options as SwcHtmlOptions } from '@swc/html';
type Options = {
    inject?: {
        data?: Record<string, string>;
        tags?: HtmlTagDescriptor[];
    };
    minify?: boolean | SwcHtmlOptions;
};
export declare const defaultMinifyOptions: SwcHtmlOptions;
export default function simpleHtmlPlugin({ minify: minifyOptions }?: Options): {
    name: string;
    transformIndexHtml: {
        order: 'post';
        handler: (html: string) => Promise<string>;
    };
};
export {};
